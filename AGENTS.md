# Agent 开发边界

本文件适用于所有在本仓库工作的 Agent，包括 Z code、MiniMax Code、Gork 和后续自动化 Agent。

## 必须遵守

1. 本文件所在的 Git 仓库根目录是唯一允许读取、修改和执行命令的工作边界。禁止访问、扫描或修改父目录、相邻目录和用户其他文件。
2. 禁止创建任何新目录。只能在任务提示词明确允许的现有目录中创建或修改文件；如果所需目录不存在，立即停止并报告缺失路径，等待项目负责人创建。
3. 禁止移动、重命名或删除任务范围外的文件。禁止把临时文件、下载物、日志、APK、AAB、密钥或环境配置写入仓库。
4. Android 构建、测试、Lint 和 APK 打包只通过 GitHub Actions 执行。禁止在本地运行 Gradle 或本地打包。
5. 开始工作前先阅读 `agent-prompts/README.md` 和对应 Agent 的提示词；只处理分配给自己的目录与文件。
6. 禁止使用 `git add .` 或 `git add -A`；只能逐个暂存已核对的任务文件。
7. 不得提交 secret、AccessKey、keystore、真实手机号、真实用户数据或生产配置。
8. 没有可访问的 GitHub Actions run URL 和绿色 Job 结果，不得声称构建、测试或打包通过。

## 分工原则

- Z code：数据模型、迁移、事务、同步、媒体、性能、安全等重要或复杂任务。
- MiniMax Code：独立 UI、设计一致性、文档、无障碍和低风险接线任务。
- Gork：仅在独立服务端仓库内按服务端文档开发，不修改本 Android 仓库代码。

用户的最新明确指令高于本文；如果新指令与本文边界冲突，先向用户确认，不自行扩大范围。
